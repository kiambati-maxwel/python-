# tuples are immutable orderd sequences of elements

dimensions = 52, 40, 100
length, width, height = dimensions #tuple unpacking 
print(f"the dimensions are {length}x{width}x{height}")

print(dimensions[1])